# Contributing

1. Use Python 3.11 or newer.
2. Install development tools with `pip install -e ".[dev]"`.
3. Run `pytest -q`, `ruff check .`, and `ruff format --check .` before release.
4. Keep release actions proof-backed and approval-gated.
5. Do not commit credentials, tokens, or private release artifacts.
